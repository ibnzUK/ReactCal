import React from 'react';

const Result = (props) => {
    return (
        <div>
             
            
              <p>{props.result}</p> 
          
           
        </div>

    );
};

export default Result;